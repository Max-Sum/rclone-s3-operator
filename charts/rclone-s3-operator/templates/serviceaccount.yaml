{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rclone-s3-operator.serviceAccountName" . }}
  labels:
    {{- include "rclone-s3-operator.labels" . | nindent 4 }}
{{- end }}
